
import { pullid } from 'simple-fit/helpers/pullid';
import { module, test } from 'qunit';

module('Unit | Helper | pullid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pullid([42]);
  assert.ok(result);
});

