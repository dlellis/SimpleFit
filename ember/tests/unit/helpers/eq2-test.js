
import { eq2 } from 'simple-fit/helpers/eq2';
import { module, test } from 'qunit';

module('Unit | Helper | eq2');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eq2([42]);
  assert.ok(result);
});

